name: Build AI review

on:
  workflow_dispatch:
    inputs:
      trip:
        description: "Canonical trip id"
        required: true
        default: "kyrgyzstan-2026"
      chapter:
        description: "Chapter slug. Legacy dayNN values are supported"
        required: true
        default: "day07"
      model:
        description: "OpenAI text model"
        required: false
        default: "gpt-4o-mini"

permissions:
  contents: write

concurrency:
  group: build-review-${{ github.event.inputs.trip }}-${{ github.event.inputs.chapter }}
  cancel-in-progress: false

jobs:
  build-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build economical AI review
        run: node build-review-v2.mjs
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TEXT_MODEL: ${{ github.event.inputs.model }}
          TRIP: ${{ github.event.inputs.trip }}
          CHAPTER: ${{ github.event.inputs.chapter }}
      - name: Validate exact photo coverage
        run: node validate-editorial-chapter.mjs
        env:
          TRIP: ${{ github.event.inputs.trip }}
          CHAPTER: ${{ github.event.inputs.chapter }}

      - name: Commit AI review
        run: |
          git config user.name "travel-journal-bot"
          git config user.email "travel-journal-bot@users.noreply.github.com"
          git add data/${{ github.event.inputs.trip }}/${{ github.event.inputs.chapter }}-ai-review.json
          if git diff --cached --quiet; then
            echo "No AI review changes"
            exit 0
          fi
          git commit -m "Build AI review for ${{ github.event.inputs.trip }} ${{ github.event.inputs.chapter }}"
          git push origin HEAD:main
