name: Build storyboard

on:
  workflow_dispatch:
    inputs:
      trip:
        description: "Canonical trip id. Example: kyrgyzstan-2026"
        required: true
        default: "kyrgyzstan-2026"
      chapter:
        description: "Chapter slug. Legacy dayNN values are supported"
        required: true
        default: "day02"
      model:
        description: "OpenAI text model for storyboard"
        required: false
        default: "gpt-4o-mini"

permissions:
  contents: write

concurrency:
  group: storyboard-${{ github.event.inputs.trip }}-${{ github.event.inputs.chapter }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Sync with latest main
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build storyboard
        run: node build-storyboard.mjs
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_TEXT_MODEL: ${{ github.event.inputs.model }}
          TRIP: ${{ github.event.inputs.trip }}
          CHAPTER: ${{ github.event.inputs.chapter }}

      - name: Validate storyboard
        run: node validate-storyboard.mjs
        env:
          TRIP: ${{ github.event.inputs.trip }}
          CHAPTER: ${{ github.event.inputs.chapter }}
      - name: Validate editorial gates
        run: node validate-editorial-chapter.mjs
        env:
          TRIP: ${{ github.event.inputs.trip }}
          CHAPTER: ${{ github.event.inputs.chapter }}

      - name: Commit storyboard
        run: |
          git config user.name "travel-journal-bot"
          git config user.email "travel-journal-bot@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Build storyboard for ${{ github.event.inputs.trip }} ${{ github.event.inputs.chapter }}"
          git push origin HEAD:main
